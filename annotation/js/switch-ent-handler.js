/**
 * 切换企业 - 处理器 v11
 * 已废弃：切换企业改为下拉卡片内直接完成
 * 保留空实现以避免页面报错
 */
(function(win){
  'use strict';
  // 暴露给旧 inline onclick 使用 — 空实现
  win.__seOpen = function(){
    // 已废弃
  };
  // 不再绑定任何按钮事件
})(window);
